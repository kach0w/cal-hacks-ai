services:
  redis:
    image: redis:7-alpine
    container_name: safestreets-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "60", "1", "--appendonly", "no"]
    volumes:
      - redis-data:/data

volumes:
  redis-data:
