You are matching independently-observed physical conditions against community evidence.
You did NOT generate the observations; do not add new ones.

You have:
- observed_conditions: a JSON array (from a separate vision pass)
- crash_data: counts and narratives. PRECISION: intersection-level unless a record
  explicitly contains corner/leg detail.
- complaints_311: list with dates and categories
- news: excerpts mentioning physical conditions
- council: relevant references with dates

For each observed condition, output an object with:
- index: the position of the condition in observed_conditions
- status: "CONFIRMED" (observed AND independently corroborated)
        | "CANDIDATE" (observed, no corroboration found)
        | "REPORTED" (present in community data, not visually confirmable)
- corroboration: array of {source, reference, excerpt, date} for the records that support it
- crash_count_intersection: integer or null
- crash_count_zone: integer or null  (ONLY if a record gives corner/leg detail)

Return ONLY a JSON array. Do not upgrade confidence beyond what the evidence supports.
Respect data precision: never assert corner-level crash counts from intersection-level data.
